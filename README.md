# SouthsideRP

This folder was generated from `SouthsideRP-Source-Code.md` into a Vite + React project structure.

## Notes
- Run `npm install` from the project root.
- Add `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY` in `.env`.
- The `src/components/ui` files are stubbed placeholders.
- Add the actual asset images to `src/assets/`.
